{
  "name": "dash-magazin-monorepo",
  "private": true,
  "type": "module",
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace main-website dev",
    "build": "yarn workspace main-website build",
    "preview": "yarn workspace main-website preview"
  }
}
